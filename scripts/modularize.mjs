import fs from 'node:fs';
import path from 'node:path';

const src = fs.readFileSync('App.jsx','utf8');
const lines = src.split(/\r?\n/);
if(lines.length < 6600 || !src.includes('export default function App(){')) throw new Error('App.jsx inesperado: refatoração abortada para proteger o código atual.');
const L=(a,b)=>lines.slice(a-1,b).join('\n')+'\n';
const W=(file,content)=>{fs.mkdirSync(path.dirname(file),{recursive:true});fs.writeFileSync(file,content)};
const tmpl=name=>{const mark='const '+name+'=`';const a=src.indexOf(mark);if(a<0)throw new Error(name+' não encontrado');const start=a+mark.length;const end=src.indexOf('`;',start);if(end<0)throw new Error(name+' sem fechamento');return src.slice(start,end).replace(/^\n/,'').replace(/\n$/,'')};

W('src/styles/global.css',tmpl('GLOBAL_CSS')+`\n.lazy-page-skeleton{min-height:280px;display:flex;align-items:center;justify-content:center;padding:40px 20px;color:#796a86;font-family:'Cinzel',serif}.lazy-page-skeleton>div{display:flex;align-items:center;gap:12px;padding:13px 18px;border-radius:12px;border:1px solid rgba(168,85,247,.16);background:rgba(8,4,18,.72);box-shadow:0 10px 34px rgba(0,0,0,.28)}.lazy-page-dot{width:9px;height:9px;border-radius:50%;background:#A855F7;box-shadow:0 0 14px rgba(168,85,247,.65);animation:pulse 1.1s ease-in-out infinite}\n`);
W('src/styles/livro.css',tmpl('LIVRO_CSS')+'\n');

W('src/core/firebase.js',`import { initializeApp } from "firebase/app";\nimport { getFirestore, enableMultiTabIndexedDbPersistence } from "firebase/firestore";\nconst firebaseConfig={apiKey:"AIzaSyAnaJjwoJ6YgGrR5pIoPrTIj7PculaIfyA",authDomain:"dinastia-e-a4b28.firebaseapp.com",projectId:"dinastia-e-a4b28",storageBucket:"dinastia-e-a4b28.firebasestorage.app",messagingSenderId:"855332392669",appId:"1:855332392669:web:9fc06c8f633bb8cce89534"};\nconst firebaseApp=initializeApp(firebaseConfig);export const db=getFirestore(firebaseApp);\nenableMultiTabIndexedDbPersistence(db).catch(err=>{if(err?.code!=='failed-precondition'&&err?.code!=='unimplemented')console.warn('Persistência offline indisponível:',err)});\n`);
let media=L(20,53).replace('function compressImage(','export function compressImage(').replace('function compressImagePNG(','export function compressImagePNG(').replace('function compressImageSmall(','export function compressImageSmall(');W('src/core/media.js',media);

let data=L(333,345)+'\n'+L(964,1147)+'\n'+L(3380,3387);for(const n of ['SHEET_COLORS','SHEET_GLOWS','MASTER_PIN','ATMOSPHERES','STATUS_LIST','CLASSES','PROLOGUE','MILESTONES','ENTITIES_DATA','ARTEFATOS_DATA','CLASS_BASE_HP','RULES_DATA','ATTRS'])data=data.replace(new RegExp(`^const ${n}=`,`m`),`export const ${n}=`);for(const n of ['getArtifactPowers','ArtifactGlyph','getClassBaseHp','getDurabilityBonus','getDurabilityHp','getSheetMaxHp'])data=data.replace(new RegExp(`^function ${n}\\(`,'m'),`export function ${n}(`);W('src/data/gameData.jsx',data);

let toast=`import { useEffect, useState } from "react";\nimport { doc, onSnapshot, setDoc } from "firebase/firestore";\nimport { db } from "./firebase";\nconst toastListeners=[];\n`+L(404,459);toast=toast.replace('function ToastContainer()','export function ToastContainer()').replace('async function pushToast(','export async function pushToast(');W('src/core/toast.jsx',toast);
let ce=`import { doc, getDoc, setDoc, updateDoc } from "firebase/firestore";\nimport { db } from "./firebase";\n`+L(460,491);ce=ce.replace('async function publishDiceResult(','export async function publishDiceResult(').replace('async function logAbilityUsed(','export async function logAbilityUsed(');W('src/core/combatEvents.js',ce);
let ui=`import { useEffect, useState } from "react";\n`+L(493,536)+'\n'+L(1150,1160);ui=ui.replace('function LevelUpScreen(','export function LevelUpScreen(').replace('function hpColor(','export function hpColor(').replace('function RestrictedAccess(','export function RestrictedAccess(')+`\nexport function btnStyle(color){return{padding:'7px 14px',borderRadius:8,border:\`1px solid \${color}55\`,background:\`\${color}18\`,color:color.startsWith('rgba')?'#6A5A7A':color,cursor:'pointer',fontFamily:'Cinzel,serif',fontSize:11,letterSpacing:'0.06em',transition:'all 0.2s'}}\n`;W('src/core/ui.jsx',ui);

let sc=L(3372,4885).replace(/\nconst ATTRS = \[\n(?:.*\n){6}\];\n/,'\n');W('src/features/sheets/SheetComponents.jsx',`import { useEffect,useRef,useState } from "react";\nimport { compressImage } from "../../core/media";\nimport { pushToast } from "../../core/toast";\nimport { logAbilityUsed } from "../../core/combatEvents";\nimport { LevelUpScreen,hpColor } from "../../core/ui";\nimport { SHEET_COLORS,SHEET_GLOWS,STATUS_LIST,CLASSES,ATTRS,getArtifactPowers,ArtifactGlyph,getClassBaseHp,getDurabilityHp,getSheetMaxHp } from "../../data/gameData";\n`+sc+`\nexport {PersonalityTags,AttrDots,VigosDots,VigosWithLocked,StatusPanel,AttrBonusStrip,resolveEquipIcon,CompactEquipSlot,CharSilhouette,CollapsibleSection,EquipamentoPanel,ArtefatoFichaPanel,CooldownBadge,HabilidadesPanel,SummonCard,InvocacoesPanel,useIsMobile,MobileSheetFull,SheetFull,newSheet};\n`);
let cm=`import { useEffect,useRef,useState } from "react";\nimport { doc,onSnapshot,setDoc,updateDoc } from "firebase/firestore";\nimport { db } from "../../core/firebase";\nimport { pushToast } from "../../core/toast";\nimport { hpColor } from "../../core/ui";\nimport { publishDiceResult } from "../../core/combatEvents";\nimport { SHEET_COLORS,STATUS_LIST,getSheetMaxHp } from "../../data/gameData";\nconst ENEMY_COLOR='#FF4444';\n`+L(537,963);cm=cm.replace('function CombatMode(','export default function CombatMode(');W('src/features/sheets/CombatMode.jsx',cm);
W('src/features/sheets/SheetsPage.jsx',`import { useEffect,useRef,useState } from "react";\nimport { collection,deleteDoc,doc,onSnapshot,setDoc } from "firebase/firestore";\nimport { db } from "../../core/firebase";\nimport { ARTEFATOS_DATA,CLASSES,SHEET_COLORS } from "../../data/gameData";\nimport { SheetFull,newSheet } from "./SheetComponents";\nimport CombatMode from "./CombatMode";\n`+L(4886,5022)+'\nexport default SheetsSection;\n');

W('src/features/prologue/ProloguePage.jsx',`import { useEffect,useRef,useState } from "react";\nimport { doc,onSnapshot,setDoc } from "firebase/firestore";\nimport { db } from "../../core/firebase";\nimport { compressImage } from "../../core/media";\nimport { PROLOGUE } from "../../data/gameData";\n`+L(3159,3240)+'\nexport default PrologueSection;\n');
W('src/features/classes/ClassesPage.jsx',`import { useEffect,useRef,useState } from "react";\nimport { collection,doc,onSnapshot,setDoc } from "firebase/firestore";\nimport { db } from "../../core/firebase";\nimport { compressImage } from "../../core/media";\nimport { CLASSES } from "../../data/gameData";\n`+L(3242,3370)+'\nexport default ClassesSection;\n');
W('src/features/mapa-mundi/MapaMundiPage.jsx',`import { useEffect,useRef,useState } from "react";\nimport { doc,onSnapshot,setDoc } from "firebase/firestore";\nimport { db } from "../../core/firebase";\nimport { compressImage } from "../../core/media";\nimport { pushToast } from "../../core/toast";\nimport { btnStyle } from "../../core/ui";\n`+L(1446,1844)+'\nexport default MapaMundiSection;\n');
W('src/features/mapa-batalha/BattleMapPage.jsx',`import { useEffect,useRef,useState } from "react";\nimport { collection,deleteDoc,doc,getDocFromServer,onSnapshot,query,setDoc,where } from "firebase/firestore";\nimport { db } from "../../core/firebase";\nimport { compressImage,compressImagePNG } from "../../core/media";\nimport { pushToast } from "../../core/toast";\nimport { CLASSES,ATTRS,SHEET_COLORS,STATUS_LIST,getSheetMaxHp } from "../../data/gameData";\nimport { hpColor } from "../../core/ui";\nimport { resolveEquipIcon,HabilidadesPanel,StatusPanel,VigosWithLocked,newSheet } from "../sheets/SheetComponents";\n`+L(1845,3048)+'\nexport default BattleMapSection;\n');
W('src/features/personagens/PersonagensPage.jsx',`import { useEffect,useRef,useState } from "react";\nimport { collection,deleteDoc,doc,onSnapshot,setDoc } from "firebase/firestore";\nimport { db } from "../../core/firebase";\nimport { compressImage } from "../../core/media";\n`+L(5225,5334)+'\nexport default PersonagensSection;\n');
W('src/features/inimigos/InimigosPage.jsx',`import { useEffect,useRef,useState } from "react";\nimport { collection,deleteDoc,doc,onSnapshot,setDoc } from "firebase/firestore";\nimport { db } from "../../core/firebase";\nimport { compressImage } from "../../core/media";\nimport { RestrictedAccess,hpColor } from "../../core/ui";\nimport { ARTEFATOS_DATA,ATTRS } from "../../data/gameData";\nimport { AttrDots,StatusPanel,ArtefatoFichaPanel,VigosDots,CooldownBadge } from "../sheets/SheetComponents";\n`+L(5016,5224)+'\n'+L(5335,5474)+'\nexport default EnemiesSection;\n');
W('src/features/bestiario/BestiarioPage.jsx',`import { useEffect,useRef,useState } from "react";\nimport { collection,deleteDoc,doc,onSnapshot,setDoc } from "firebase/firestore";\nimport { db } from "../../core/firebase";\nimport { compressImage } from "../../core/media";\n`+L(5475,5640)+'\nexport default BestiarioSection;\n');
W('src/features/regras/RegrasPage.jsx',`import { useState } from "react";\nimport { RULES_DATA } from "../../data/gameData";\n`+L(5641,5773)+'\nexport default RulesSection;\n');
W('src/features/livro/LivroPage.jsx',`import { useEffect,useRef,useState } from "react";\nimport { collection,deleteDoc,doc,onSnapshot,setDoc } from "firebase/firestore";\nimport { db } from "../../core/firebase";\nimport { MILESTONES,ENTITIES_DATA,ARTEFATOS_DATA,getArtifactPowers,ArtifactGlyph } from "../../data/gameData";\n`+L(5774,5949)+'\nexport default LibroSection;\n');
W('src/features/cronicas/CronicasPage.jsx',`import { useEffect,useRef,useState } from "react";\nimport { collection,deleteDoc,doc,onSnapshot,setDoc } from "firebase/firestore";\nimport { db } from "../../core/firebase";\nimport { compressImage,compressImageSmall } from "../../core/media";\nimport { btnStyle } from "../../core/ui";\n`+L(5950,6331)+'\nexport default CronicasSection;\n');

let star=`import { useEffect,useRef } from "react";\nimport { ATMOSPHERES } from "../data/gameData";\n`+L(1148,1149);star=star.replace('function StarField(','export default function StarField(');W('src/shell/StarField.jsx',star);
const cats=L(1161,1169);let amb=`import { useEffect,useRef,useState } from "react";\nimport { doc,getDocFromServer,onSnapshot,setDoc } from "firebase/firestore";\nimport { db } from "../core/firebase";\nimport { pushToast } from "../core/toast";\n`+cats+L(1264,1445);amb=amb.replace('function AmbientSoundPlayer(','export default function AmbientSoundPlayer(');W('src/shell/AmbientSoundPlayer.jsx',amb);
let dice=`import { useEffect,useRef,useState } from "react";\nimport { doc,onSnapshot } from "firebase/firestore";\nimport { db } from "../core/firebase";\nimport { publishDiceResult } from "../core/combatEvents";\n`+L(3049,3158);dice=dice.replace('function DiceTrayVisual(','export function DiceTrayVisual(')+`\nexport default DiceWidget;\n`;W('src/shell/DiceWidget.jsx',dice);
let mt=`import { useState } from "react";\nimport { MASTER_PIN } from "../data/gameData";\n`+L(6395,6401)+'\nexport default MasterToggle;\n';W('src/shell/MasterToggle.jsx',mt);
W('src/shell/PlayerCombatBanner.jsx',`import { useEffect,useState } from "react";\nimport { doc,onSnapshot } from "firebase/firestore";\nimport { db } from "../core/firebase";\n`+L(6402,6552)+'\nexport default PlayerCombatBanner;\n');
W('src/shell/PublicDiceOverlay.jsx',`import { useEffect,useRef,useState } from "react";\nimport { doc,onSnapshot } from "firebase/firestore";\nimport { db } from "../core/firebase";\nimport { DiceTrayVisual } from "./DiceWidget";\n`+L(6553,6607)+'\nexport default PublicDiceOverlay;\n');
W('src/shell/ConnectionStatus.jsx',`import { useEffect,useState } from "react";\n`+L(6608,6617)+'\nexport default ConnectionStatus;\n');

W('src/App.jsx',`import { lazy,Suspense,useEffect,useState } from "react";\nimport { doc,onSnapshot } from "firebase/firestore";\nimport { db } from "./core/firebase";\nimport { ATMOSPHERES } from "./data/gameData";\nimport "./styles/global.css";import "./styles/livro.css";\nimport StarField from "./shell/StarField";import { ToastContainer } from "./core/toast";import PublicDiceOverlay from "./shell/PublicDiceOverlay";import ConnectionStatus from "./shell/ConnectionStatus";import MasterToggle from "./shell/MasterToggle";import AmbientSoundPlayer from "./shell/AmbientSoundPlayer";import PlayerCombatBanner from "./shell/PlayerCombatBanner";import DiceWidget from "./shell/DiceWidget";\nconst pageLoaders={prologo:()=>import("./features/prologue/ProloguePage"),classes:()=>import("./features/classes/ClassesPage"),fichas:()=>import("./features/sheets/SheetsPage"),personagens:()=>import("./features/personagens/PersonagensPage"),inimigos:()=>import("./features/inimigos/InimigosPage"),bestiario:()=>import("./features/bestiario/BestiarioPage"),regras:()=>import("./features/regras/RegrasPage"),livro:()=>import("./features/livro/LivroPage"),cronicas:()=>import("./features/cronicas/CronicasPage"),mapamundi:()=>import("./features/mapa-mundi/MapaMundiPage"),mapabatalha:()=>import("./features/mapa-batalha/BattleMapPage")};\nconst LazyPages=Object.fromEntries(Object.entries(pageLoaders).map(([k,v])=>[k,lazy(v)]));\nconst TABS=[{id:'prologo',label:'Prólogo',icon:'📜'},{id:'classes',label:'Classes',icon:'⚔️'},{id:'fichas',label:'Fichas',icon:'📋'},{id:'personagens',label:'Personagens',icon:'👤'},{id:'inimigos',label:'Inimigos',icon:'💀'},{id:'bestiario',label:'Bestiário',icon:'🐉'},{id:'regras',label:'Regras',icon:'📖'},{id:'livro',label:'Livro da Mandíbula',icon:'✦'},{id:'cronicas',label:'Crônicas',icon:'🗒️'},{id:'mapamundi',label:'Mapa Múndi',icon:'🌍'},{id:'mapabatalha',label:'Mapa de Batalha',icon:'🗡️'}];\nconst Loading=()=> <div className="lazy-page-skeleton"><div><span className="lazy-page-dot"/> Abrindo registro...</div></div>;\nexport default function App(){const[tab,setTab]=useState('prologo');const[masterMode,setMasterMode]=useState(false);const[atmosphere,setAtmosphere]=useState('neutro');useEffect(()=>{const u=onSnapshot(doc(db,'config','atmosphere'),s=>{if(s.exists())setAtmosphere(s.data().key||'neutro')});return()=>u()},[]);const atm=ATMOSPHERES[atmosphere]||ATMOSPHERES.neutro;const lock=tab==='mapabatalha';const Page=LazyPages[tab]||LazyPages.prologo;const prefetch=id=>pageLoaders[id]?.().catch(()=>{});return <div style={{height:'100vh',overflow:'hidden',display:'flex',flexDirection:'column',background:atm.bg,color:'#C8B8A0',fontFamily:"'Crimson Text',Georgia,serif",position:'relative',transition:'background 1.2s'}}><StarField atmosphere={atmosphere}/><ToastContainer/><PublicDiceOverlay/><ConnectionStatus/><header style={{position:'relative',zIndex:10,borderBottom:'1px solid rgba(255,255,255,0.05)',background:'linear-gradient(180deg,rgba(8,3,18,0.99),rgba(5,2,12,0.95))',backdropFilter:'blur(8px)'}}><div style={{display:'flex',alignItems:'center',justifyContent:'space-between',padding:'14px 18px 10px'}}><div style={{width:80}}/><div style={{textAlign:'center',flex:1}}><div className="header-sub" style={{fontSize:9,letterSpacing:'0.5em',color:'#4A3A5A',fontFamily:'Cinzel,serif',marginBottom:4,textTransform:'uppercase'}}>Cosmum · O Livro da Mandíbula · Vigor Cósmico</div><h1 className="header-title" style={{fontFamily:'Cinzel Decorative,serif',fontSize:22,fontWeight:900,margin:0,letterSpacing:'0.08em',background:'linear-gradient(135deg,#C8A8E8,#E8D8C0,#A855F7)',WebkitBackgroundClip:'text',WebkitTextFillColor:'transparent'}}>Dinastia E</h1><div className="header-sub" style={{fontSize:10,color:'#4A3A5A',fontFamily:'Cinzel,serif',marginTop:2,letterSpacing:'0.15em'}}>Livro do Mundo</div></div><div style={{width:180,display:'flex',justifyContent:'flex-end',alignItems:'center',gap:8}}><MasterToggle masterMode={masterMode} setMasterMode={setMasterMode}/></div></div></header><nav style={{position:'relative',zIndex:10,display:'flex',justifyContent:'center',gap:3,padding:'9px 14px',background:'rgba(6,2,14,0.94)',borderBottom:'1px solid rgba(255,255,255,0.04)',flexWrap:'wrap'}}>{TABS.map(t=><button key={t.id} onClick={()=>setTab(t.id)} onMouseEnter={()=>prefetch(t.id)} onFocus={()=>prefetch(t.id)} onPointerDown={()=>prefetch(t.id)} style={{padding:'6px 12px',borderRadius:6,cursor:'pointer',fontFamily:'Cinzel,serif',fontSize:11,letterSpacing:'0.06em',border:tab===t.id?`1px solid ${atm.accent}66`:'1px solid transparent',background:tab===t.id?`${atm.accent}14`:'transparent',color:tab===t.id?atm.accent:'#5A4A6A',transition:'all 0.3s'}}>{t.icon} {t.label}</button>)}</nav><main className={lock?'main-locked':''} style={{flex:1,overflowY:lock?'hidden':'auto',position:'relative',zIndex:10,scrollBehavior:'smooth',display:lock?'flex':'block',flexDirection:'column'}}><div key={tab} style={lock?{animation:'pageTurn 0.5s cubic-bezier(0.2,0.8,0.2,1)',flex:1,minHeight:0,display:'flex',flexDirection:'column'}:{animation:'pageTurn 0.5s cubic-bezier(0.2,0.8,0.2,1)'}}><Suspense fallback={<Loading/>}><Page masterMode={masterMode}/></Suspense></div></main><AmbientSoundPlayer masterMode={masterMode}/><PlayerCombatBanner/><DiceWidget/></div>}\n`);
W('App.jsx','export { default } from "./src/App.jsx";\n');
W('vite.config.js',`import { defineConfig } from 'vite'\nimport react from '@vitejs/plugin-react'\nexport default defineConfig({plugins:[react()],build:{target:'es2020',cssCodeSplit:true,sourcemap:false,chunkSizeWarningLimit:700,rollupOptions:{output:{manualChunks(id){if(!id.includes('node_modules'))return;if(id.includes('/firebase/'))return'vendor-firebase';if(id.includes('/react/')||id.includes('/react-dom/'))return'vendor-react';return'vendor'}}}},optimizeDeps:{include:['react','react-dom','firebase/app','firebase/firestore']}})\n`);
W('ARCHITECTURE.md',`# Dinastia E — arquitetura modular\n\nCada aba é carregada sob demanda com React.lazy. Firebase é centralizado em src/core/firebase.js; dados estáticos em src/data/gameData.jsx; estilos em src/styles; componentes compartilhados de fichas em src/features/sheets/SheetComponents.jsx. Listeners específicos vivem dentro de cada feature e são desmontados ao sair da aba. O mapa de batalha fica isolado para não renderizar o restante do site durante movimentação de tokens. Imagens existentes continuam no formato atual; migração para Firebase Storage deve ser uma etapa separada para preservar dados.\n`);
console.log('Refatoração modular gerada com sucesso.');
